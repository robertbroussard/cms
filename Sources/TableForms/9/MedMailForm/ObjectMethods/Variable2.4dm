  // Object Method for vEnvelopes in Form [MediatorFile];"MedMailForm"
  // select if want envelopes
  // Dialog initializes check box to 0, so save variables for 2nd Dialog
  // 
vEnvelope:=1
vLabel:=0
vSavSwEnv:=1
vSavSwLbl:=0